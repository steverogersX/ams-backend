# Server
NODE_ENV=development
PORT=3000

# CORS (comma-separated list of allowed origins, or * for all)
CORS_ORIGIN=*

# Database (PostgreSQL connection string)
DATABASE_URL=postgres://user:pass@localhost:5432/ams
